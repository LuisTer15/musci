<h1><%= @artist.name %></h1>
<h3>Albumes (<%= @albums.length %>)</h3>
<table>
  <% @albums.each do |album| %>
    <tr>
      <td><img src="<%= album.image_url %>"></td>
      <td><%= album.name %></td>
      <td><%= album.released_at %></td>
    </tr>
  <% end %>
</table>