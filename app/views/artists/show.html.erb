<table>
  <% @albums.each do |album| %>
    <tr>
      <td><img src="<%= album.image_url %>"></td>
      <td><%= album.name %></td>
      <td><%= album.released_at %></td>
    </tr>
  <% end %>
</table>